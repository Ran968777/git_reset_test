1. 首次提交, master分支,跟再此处创建dev , feat分支
2. 切换到dev分支, 修改内容,再次提交(这里记录为一个checkpoint)
3. 切换到feat 分支,修改内容, push 到 远程 ,merge 到dev分支. push到远程
4. 此时发现dev的代码有问题,切回feat 分支, reset -soft回滚,修改内容后,git push -f 推送分支
5. 切换dev分支, merge 最新的feat分支 into dev.然后再git push -f dev分支
6
7. Q: 这样操作会丢失第2步中的checkpoint的修改内容吗
8
9. A: **不会丢失**。
10. 
11. **原因分析**：
12. 1. **checkpoint 在 dev 分支上**：第2步中，checkpoint 是直接提交在 `dev` 分支上的（例如 Commit D1）。
13. 2. **feat 分支的回滚与 dev 无关**：第4步中，`feat` 分支进行了 `reset` 和 `force push`，这只改变了 `feat` 分支的历史。虽然 `dev` 分支之前 merge 过的旧 `feat` 提交（F1）可能在 `feat` 分支上找不到了，但它已经作为历史的一部分存在于 `dev` 分支中（通过 Merge Commit M1）。
14. 3. **再次 Merge 只会增加历史**：第5步中，将新的 `feat` 分支（F2）merge 到 `dev`，这会在 `dev` 现有历史（包含 D1 和 M1）的基础上创建一个新的 Merge Commit（M2）。
15. 
16. 因此，`dev` 分支的历史将包含：
17. - 最初的 Checkpoint (D1)
18. - 第一次 Merge 的结果 (M1)
19. - 第二次 Merge 的结果 (M2)
20. 
21. **注意**：虽然 checkpoint 不会丢失，但由于 `feat` 分支经历了“重写历史”，在第5步 merge 时极大概率会遇到**代码冲突**（因为 Git 会看到两个不同的历史分支修改了相同的内容）。你需要手动解决冲突，但 Checkpoint 的内容本身是安全的。
