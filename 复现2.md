1. 首次提交, master分支,跟再此处创建dev , feat分支
2. 切换到dev分支, 修改内容,再次提交(这里记录为一个checkpoint)
3. 切换到feat 分支,修改内容, push 到 远程 ,merge 到dev分支. push到远程
4. 此时发现dev的代码有问题,切回feat 分支, reset -soft回滚,修改内容后,git push -f 推送分支
5. 切换dev分支. 然后再git push -f dev分支
6
7. Q: 这样操作会丢失第2步中的checkpoint的修改内容吗
8
9. A: **不会丢失**。
10. 
11. **原因分析**：
12. 1. **checkpoint 依然存在于 dev 分支**：在第 2 步，checkpoint 被提交到了 `dev` 分支。此后的操作中，**没有任何步骤对 dev 分支进行了回退（reset）**。
13. 2. **feat 的回滚不影响 dev**：第 4 步的操作（`reset` 和 `push -f`）仅针对 `feat` 分支。虽然 `feat` 分支的历史被重写了，但 `dev` 分支依然停留在第 3 步结束时的状态（即包含 Checkpoint 和 旧的 feat 合并提交）。
14. 3. **push -f dev 只是同步**：第 5 步切换回 `dev` 分支后，直接执行 `git push -f dev`。由于本地的 `dev` 分支并没有被修改或回退，它依然包含 Checkpoint。这个操作仅仅是将本地的状态强制同步给远程（实际上如果远程没变，这就是一个空操作）。
15. 
16. **结论**：只要你没有在 `dev` 分支上执行 `git reset`，无论你在其他分支（如 `feat`）上做什么操作，`dev` 分支上的 Checkpoint 都是安全的。
